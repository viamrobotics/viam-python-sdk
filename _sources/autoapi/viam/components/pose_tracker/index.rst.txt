:py:mod:`viam.components.pose_tracker`
======================================

.. py:module:: viam.components.pose_tracker


Submodules
----------
.. toctree::
   :titlesonly:
   :maxdepth: 1

   client/index.rst
   pose_tracker/index.rst
   service/index.rst


Package Contents
----------------

Classes
~~~~~~~

.. autoapisummary::

   viam.components.pose_tracker.PoseTrackerClient
   viam.components.pose_tracker.PoseTracker




.. py:class:: PoseTrackerClient(name: str, channel: grpclib.client.Channel)



   gRPC client for the PoseTracker component.

   .. py:method:: get_poses(self, body_names: List[str]) -> Dict[str, viam.proto.api.common.PoseInFrame]
      :async:

      Returns the current pose of each body tracked by the pose tracker.

      :param body_names: Names of the bodies whose poses are being
                         requested. In the event this parameter is not supplied or is
                         an empty list, all available poses are returned.
      :type body_names: List[str]


   .. py:method:: do(self, command: Dict[str, Any]) -> Dict[str, Any]
      :async:

      Send/Receive arbitrary commands

      :param command: The command to execute
      :type command: Dict[str, Any]

      :raises NotImplementedError: Raised if the component does not support arbitrary commands

      :returns: Result of the executed command
      :rtype: Dict[str, Any]



.. py:class:: PoseTracker(name: str)



   PoseTracker represents a physical pose or motion tracking device.

   This acts as an abstract base class for any drivers representing specific
   pose tracker implementations. This cannot be used on its own. If the `__init__()` function is
   overriden, it must call the `super().__init__()` function.

   .. py:method:: get_poses(self, body_names: List[str]) -> Dict[str, viam.proto.api.common.PoseInFrame]
      :abstractmethod:
      :async:

      Returns the current pose of each body tracked by the pose tracker.

      :param body_names: Names of the bodies whose poses are being
                         requested. In the event this parameter is not supplied or is
                         an empty list, all available poses are returned.
      :type body_names: List[str]



